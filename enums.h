#ifndef ENUMS_H
#define ENUMS_H

typedef enum Columns
{
	ColumnText,
	ColumnCount
} Columns;

typedef enum ObjectType
{
	COLLECTION_SERVERS,
	COLLECTION_DATABASES,
	COLLECTION_USERS,
	COLLECTION_TABLESPACES,
	COLLECTION_GROUPS,

	OBJECT_SERVER,
	OBJECT_DATABASE,
	OBJECT_USER,
	OBJECT_TABLESPACE,
	OBJECT_GROUP
} ObjectType;

#endif // ENUMS_H
